import { css } from 'react-emotion';

const formStyle = css`
  input,
  textarea,
  select {
    box-sizing: border-box;
  }

  input {
    padding: 7px 14px;
  }

  select {
    background-color: #fff;
    padding: 10px;
  }
`;

export default formStyle;
